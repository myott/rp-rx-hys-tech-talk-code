buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'org.ajoberstar:gradle-git:1.0.0'
  }
}

apply plugin: 'org.ajoberstar.github-pages'

githubPages {
  repoUri = 'git@github.com:myott/rp-rx-hys-tech-talk.git'
  targetBranch = 'gh-pages'
  pages {
    from(file("$rootDir/build/asciidoc/revealjs")) {
      into "."
    }
  }
}
